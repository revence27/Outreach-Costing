%h4 Settings
.assumptions
  %table
    %thead
      %th
        Name
      %th
        Value
      %th
        Units
    - @assumptions.each do |assumption|
      %tr{:class => 'assumption', :id => %[assumption_#{assumption.id}]}
        %td
          = assumption.name
        %td
          = assumption.value
        %td
          = assumption.units
